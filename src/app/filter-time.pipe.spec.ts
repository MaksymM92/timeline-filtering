import { FilterTimePipe } from './filter-time.pipe';

describe('FilterTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTimePipe();
    expect(pipe).toBeTruthy();
  });
});
